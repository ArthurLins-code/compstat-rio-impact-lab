# CompStat Rio — dependências Python consolidadas (backend + normalização).
#
# Use:
#   pip install -r requirements.txt
#
# Versão é minima compativel; gere um lock (pip freeze) quando o ambiente
# da Prefeitura estabilizar (Fase 5 do PLANO_PRODUTIZACAO).

# --- Backend (FastAPI + IA) ---
fastapi>=0.110
uvicorn[standard]>=0.27
sse-starlette>=2.0
pydantic>=2.0
python-dotenv>=1.0
anthropic>=0.40

# --- Dados / geoespacial ---
duckdb>=1.0
pandas>=2.0
numpy>=1.26
shapely>=2.0
pyshp>=2.3

# --- Excel / Word ---
openpyxl>=3.1
python-docx>=1.1

# --- Persistência (Fase 2) ---
sqlalchemy>=2.0
